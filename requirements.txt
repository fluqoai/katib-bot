# Kateb — Python dependencies
# Pinned to the major.minor version. Patch versions float.

# Web server (FastAPI)
fastapi==0.115.4
uvicorn[standard]==0.32.0
# python-multipart is required for FastAPI's File / Form parsing
# (used by /api/admin/upload and other multipart endpoints). FastAPI
# raises RuntimeError at startup if it's not installed.
python-multipart==0.0.20

# Telegram bot (v21+ has native asyncio) — optional, runs alongside web
python-telegram-bot[ext]==21.6

# Supabase
supabase==2.8.1

# HTTP + async
httpx==0.27.2
aiohttp==3.10.10

# Google Drive (optional path)
google-api-python-client==2.154.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Embeddings + LLM (via OpenRouter, but the OpenAI client works for the
# OpenAI-compatible /embeddings endpoint if needed)
openai==1.55.0
google-generativeai==0.8.3

# Document parsing
python-docx==1.1.2          # .docx text extraction
pymupdf==1.24.13            # .pdf text extraction (fitz)
# tiktoken — token counting for the chunker (256-token hard cap)
tiktoken==0.12.0

# Pydantic + settings
pydantic==2.9.2
pydantic-settings==2.6.1

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.2
mypy==1.13.0
